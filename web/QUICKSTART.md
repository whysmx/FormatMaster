# Web版快速开始指南

## 🚀 5分钟上手

### 第一步：安装依赖

```bash
cd web
pip install -r requirements.txt
```

### 第二步：上传第一个模板

#### 方式1: 使用命令行（快速测试）

```bash
# 复制测试模板
cp ../examples/正常格式.docx template_files/test_template.docx

# 创建模板配置
cat > data/templates.json << EOF
{
  "templates": [
    {
      "id": "test-001",
      "name": "标准合同格式",
      "filename": "test_template.docx",
      "is_default": true,
      "updated_at": "2024-01-08T10:00:00"
    }
  ]
}
EOF
```

#### 方式2: 通过Web界面上传（推荐）

1. 启动服务后访问 http://localhost:8000
2. 点击"模板管理"页面
3. 点击"+ 上传新模板"
4. 填写模板名称并选择文件

### 第三步：启动服务

```bash
# 使用启动脚本
./start.sh

# 或直接运行
python3 main.py
```

### 第四步：开始使用

1. 打开浏览器访问 `http://localhost:8002`
2. 选择一个模板
3. 上传待处理的Word文档
4. 点击"开始转换"
5. 下载转换后的文件

---

## 📸 界面预览

### 文档转换页面

```
┌─────────────────────────────────┐
│   📄 Word格式还原工具            │
├─────────────────────────────────┤
│  1. 选择模板                     │
│  ┌───────────────────────────┐  │
│  │ ⭐ 标准合同格式         ✓ │  │  ← 选中状态
│  │    更新: 2024-01-08       │  │
│  │                           │  │
│  │   报告格式                │  │
│  │    更新: 2024-01-07       │  │
│  └───────────────────────────┘  │
│                                 │
│  2. 上传文档                    │
│  [📁 点击选择文件]               │
│                                 │
│  3. 开始转换                    │
│  [开始转换]                     │
│                                 │
│  ✅ 转换完成！                   │
│  格式相似度: 100%                │
│  [下载文件]                     │
└─────────────────────────────────┘
```

### 模板管理页面

```
┌─────────────────────────────────┐
│   📋 模板管理                    │
├─────────────────────────────────┤
│  [+ 上传新模板]                 │
│                                 │
│  ┌───────────────────────────┐  │
│  │ ⭐ 标准合同格式(默认)     │  │
│  │    更新: 2024-01-08       │  │
│  │    [删除]                │  │
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │   报告格式                │  │
│  │    更新: 2024-01-07       │  │
│  │    [设为默认] [删除]     │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

---

## ✅ 功能测试

### 测试1: 上传模板

1. 访问 http://localhost:8002/manage
2. 点击"+ 上传新模板"
3. 填写信息：
   - 模板名称: `测试模板`
   - 选择文件: 选择一个.docx文件
   - 勾选"设为默认模板"
4. 点击"上传"

### 测试2: 转换文档

1. 访问 http://localhost:8000
2. 选择刚才上传的模板
3. 上传一个需要格式化的.docx文件
4. 点击"开始转换"
5. 等待转换完成
6. 点击"下载文件"

### 测试3: 文件隔离

1. 在浏览器A中上传一个文件并转换
2. 在浏览器B中尝试访问A的下载链接（应该失败）

---

## 🔧 故障排除

### 问题1: 无法启动服务

**错误**: `ModuleNotFoundError: No module named 'fastapi'`

**解决**:
```bash
pip install -r requirements.txt
```

### 问题2: 模板无法上传

**错误**: `500 Internal Server Error`

**检查**:
1. 确保上传的是.docx文件
2. 检查 `template_files/` 目录是否存在且有写入权限

### 问题3: 转换失败

**错误**: `转换失败: xxx`

**检查**:
1. 模板文件是否有效
2. 上传的文件是否是.docx格式
3. 查看控制台错误日志

### 问题4: 下载失败

**错误**: `文件不存在或已过期`

**原因**: 文件已下载一次，被自动删除

**解决**: 重新转换文档

---

## 💡 使用技巧

### 1. 批量处理

虽然每次只能处理一个文件，但可以：
1. 准备好所有待处理文件
2. 依次上传转换
3. 逐个下载结果

### 2. 模板管理

- 给模板起清晰的名字（如：合同模板_v1.0）
- 定期清理不用的模板
- 保留一个默认模板方便使用

### 3. 文件命名

转换后的文件会自动添加 `_已还原` 后缀：
- 原文件: `合同草稿.docx`
- 输出文件: `合同草稿_已还原.docx`

---

## 🌐 局域网访问

### 让其他电脑访问

1. 查看服务器IP地址（假设为 `192.168.1.100`）
2. 启动服务：
   ```bash
   python3 main.py
   # 或
   uvicorn main:app --host 0.0.0.0 --port 8000
   ```
3. 在其他电脑访问：`http://192.168.1.100:8000`

### 防火墙设置

**Ubuntu/Debian**:
```bash
sudo ufw allow 8000
```

**CentOS/RHEL**:
```bash
sudo firewall-cmd --add-port=8002/tcp --permanent
sudo firewall-cmd --reload
```

**Windows**:
- 控制面板 → Windows防火墙 → 入站规则
- 新建规则允许端口8000

---

## 📊 性能优化

### 1. 增加并发处理

```bash
uvicorn main:app --workers 4
```

### 2. 设置文件大小限制

在 `main.py` 中添加：
```python
app.add_middleware(
    ...,
    max_request_size=100 * 1024 * 1024  # 100MB
)
```

### 3. 定期清理临时文件

创建清理脚本：
```bash
#!/bin/bash
find static/uploads/ -type f -mtime +1 -delete
```

---

## 🎓 下一步

- 查看 [README.md](README.md) 了解更多功能
- 查看 [../docs/需求文档_Word格式还原工具.md](../docs/需求文档_Word格式还原工具.md) 了解技术细节

---

**祝您使用愉快！** 🎉
